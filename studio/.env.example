SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production
# Must match the Next.js app URL used in the Presentation iframe
SANITY_STUDIO_PREVIEW_ORIGIN=http://localhost:3000

# Optional: same Viewer token Next uses for draft mode + live preview (see web/.env.example).
# SANITY_API_READ_TOKEN=
