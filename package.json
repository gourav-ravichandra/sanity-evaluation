{
  "name": "sanity-evaluation-monorepo",
  "private": true,
  "description": "Root package.json so Vercel detects Next.js when the connected Git root is the monorepo root. The app source lives in web/.",
  "dependencies": {
    "next": "^16.0.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  }
}
