import type { ReactNode } from 'react'

import './case-study-landing.css'

export const revalidate = 60

export default function LandingPagesLayout({ children }: { children: ReactNode }) {
  return children
}
